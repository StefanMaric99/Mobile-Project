import React from "react";
import { StyleSheet, Text, View } from 'react-native';

export default function Weather() {
	React.useEffect(() => {
			
	}, []);
	
	return (
		<View>
			<Text>Weather</Text>
		</View>
	);
}